{
  "name": "repenser-la-famille",
  "version": "0.1.0",
  "dependencies": {
    "less": "^3.12.2"
  },
  "scripts": {
    "run-dev": "python3 -m http.server 8080 --bind 127.0.0.1",
    "build-dev": "nodemon --exec ./render.sh -e less"
  }
}
